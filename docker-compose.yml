services:
  mongodb:
    image: mongo:8
    container_name: kaushal-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: kaushal
    volumes:
      - kaushal-mongodb-data:/data/db
