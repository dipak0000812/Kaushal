/**
 * Application-wide constants required by the foundation layer.
 * Domain-specific constants (application status, state machine, etc.)
 * will be added here as those modules are implemented.
 */

/** API version prefix — matches the base path in API_Contract.md */
export const API_PREFIX = '/api/v1';
